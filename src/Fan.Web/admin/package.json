{
  "name": "fanray-admin",
  "version": "1.1.0",
  "scripts": {
    "build": "babel js --out-dir ../wwwroot/admin/js --source-maps",
    "build:w": "babel js --out-dir ../wwwroot/admin/js --source-maps --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-minify": "^0.4.3"
  }
}